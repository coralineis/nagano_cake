    <div class="col-2 mt-3">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tboddy>
          <% genres.each do |genre| %>
            <tr>
              <td><%= link_to genre.name, genre_path(genre.id), class: "text-dark" %></td>
            </tr>
          <% end %>
        </tboddy>
      </table>
    </div>
